# 播放清單匯入實作備註

## 匯入策略

Apple Music 的公開歌單沿用既有頁面所使用的跨來源讀取流程：將貼上的公開 `music.apple.com` 歌單 URL 經既有代理取回，再從 `serialized-server-data` 和可見 HTML 內的結構資料整理歌曲。

Spotify 沒有可直接在靜態前端安全使用的、免驗證官方曲目端點；官方 Web API 的播放清單曲目請求需要應用程式或使用者憑證。因此，介面會先以既有跨來源讀取層擷取公開 `open.spotify.com/playlist/…` 頁面，並使用容錯的結構遞迴解析器辨識可公開取得的曲目資料。若 Spotify 更改頁面資料結構或封鎖公共讀取，介面將保留輸入 URL 並提供可理解的失敗訊息，不會以假資料替代。

## 互動流程

| 步驟 | 行為 | 結果 |
| --- | --- | --- |
| 選擇服務 | Apple Music 或 Spotify | 輸入提示、顏色與驗證規則同步切換 |
| 貼上 URL | 接受完整公開歌單 URL 或 Spotify 分享短連結 | 先展開短連結，再執行服務比對 |
| 擷取歌曲 | 讀取並解析公開頁面資料 | 以既有歌曲列呈現、支援中文篩選與 Telegram 傳送 |
| 錯誤處理 | URL 無效、服務不相符、頁面無可解析曲目 | 不清除貼上內容，將具體原因寫進操作日誌 |

## 已驗證觀察

在未登入的瀏覽器環境中，Spotify 公開播放清單頁面未提供可直接讀取的歌曲 DOM；嘗試取得 Web Player 短期憑證的請求回傳 403。不過，對同一公開歌單的 Spotify 嵌入頁面可提供含曲目 `uri`、`title`、`subtitle` 與 `audioPreview.url` 的結構資料。因此實作會先讀取公開歌單頁面，若未解析到曲目便自動切換到公開嵌入頁面；整個流程均不儲存或嵌入 Spotify 帳戶憑證。

在本機預覽的實際頁面中，原有 Apple Music「必聽新歌」流程已成功擷取 100 首歌曲；Apple Music、Spotify 來源鍵，以及公開歌單 URL 輸入欄與擷取按鈕均同時存在。
