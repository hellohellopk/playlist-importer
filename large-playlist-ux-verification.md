# 大型歌單體驗驗證紀錄

## 本機預覽（2026-08-22）

- Apple Music「必聽新歌」完整載入 **496 首**後，工作區顯示「完整曲目已整理完成」與 **496 首已就緒**摘要。
- CSV、JSON、TXT 匯出控制仍以完整 **496 首**資料啟用，不受列表分段顯示影響。
- 歌曲列表的初始渲染採 **120 首**一組，後續曲目由「顯示下一組」控制載入，以避免大型歌單一次建立過多資料列。
- 已實際觸發「顯示下一組」，列表由 **120／496 首**擴展至 **240／496 首**，而 CSV、JSON、TXT 控制仍維持完整 **496 首**匯出範圍。

## GitHub Pages（2026-08-22）

使用公開 Spotify 歌單「我的播放清單 #1」測試時，網站的處理進度會顯示為逐頁讀取，完成後顯示 **1,362 首已就緒**。近期匯入區會自動新增可重新讀取的 Spotify 項目；CSV、JSON、TXT 匯出按鈕維持 **1,362 首**完整資料範圍，而列表初始只建立首批資料列。

已在近期匯入區點選「我的播放清單 #1」執行重載。重載期間會暫時停用匯出，完成後再次顯示 **1,362 首**並重新啟用 CSV、JSON、TXT 的完整匯出按鈕，確認近期紀錄可作為安全的一鍵重新讀取入口。

重載後已實際下載 `我的播放清單-#1-2026-08-22.json`；下載紀錄顯示檔案由 GitHub Pages 產生，且網站操作日誌確認其內容對應 **1,362 首**歌曲。
