# 多選與批量操作驗證紀錄

## 本機預覽（2026-08-22）

Apple Music「必聽新歌」載入 496 首後，匯出區顯示第三種「已選歌曲 0」範圍；多選工具列同步顯示「全選目前篩選 496」、「清除」與「批量複製 0」。每首歌曲列已出現獨立的可存取核取方塊，且既有單首開啟、複製與 Telegram 操作仍保留。

勾選前三首後，工具列、已選匯出範圍均更新為 **3 首**；以實際按鈕觸發批量複製後，操作日誌顯示「完成：已批量複製 3 首歌曲的資訊與連結。」

切換匯出範圍至「已選歌曲 3」後，CSV、JSON、TXT 三個按鈕均更新為三首範圍；實際下載 JSON 後，操作日誌確認「完成：已匯出已選歌曲 3 首歌曲為 JSON。」

隨後下載 CSV 與 TXT，操作日誌分別確認「已匯出已選歌曲 3 首歌曲為 CSV」及「已匯出已選歌曲 3 首歌曲為 TXT」。因此三種格式均只使用已勾選的三首歌曲。

行動版檢查發現第三種匯出範圍會使歌曲工作區標題可用寬度不足，已改為標題與工具列上下排列。修正後，歌曲標題、完整／篩選／已選三種匯出範圍及多選工具列都能在 375px 寬度完整呈現。

## 分段清單驗證

修正後重新載入 496 首歌單時，多選按鈕顯示「全選目前顯示 120」，而非錯誤地選取全部 496 首。這使全選行為與初始分段渲染範圍一致。

點選全選後，已選範圍與批量複製均更新為 **120 首**，操作日誌也明確記錄「已選取目前顯示的 120 首歌曲」。這確認初始分段不會誤選尚未顯示的 376 首。

展開下一組後，可見歌曲數更新為 **240 首**，原先選取的 120 首維持不變；再次點選「全選目前顯示 240」後，已選範圍與批量複製均更新為 **240 首**。此流程證實多選會依實際已渲染的分段範圍運作。

切換至「已選歌曲 240」後，JSON 匯出按鈕提示與操作日誌均確認輸出 **240 首**，而非完整歌單的 496 首。

在同一個 240 首已選範圍下，批量複製完成提示顯示 240 首，CSV 匯出完成提示亦顯示 240 首，驗證大型分段選取可安全套用至複製與 CSV 輸出。

TXT 匯出完成提示同樣顯示 **240 首**。因此已完成大型分段下的批量複製與 CSV、JSON、TXT 三種已選輸出驗證。

最後點選清除後，已選範圍與批量複製皆同步回到 **0 首**，操作日誌顯示「已清除歌曲選取。」；此驗證完成多選的全選、輸出與清除流程。
