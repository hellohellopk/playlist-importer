# 完整曲目分頁調查

Apple Music 公開頁面在首批資料後提供下一頁 `next` 路徑，例如 `offset=100`，但該續頁端點需 Apple Music 網頁播放器的請求標頭與短期權杖。實際在應用程式來源中測試，Apple Music 的前端腳本及續頁 API 均因跨來源限制而無法由瀏覽器直接讀取。因此，完整分頁不能安全地在既有純前端直接呼叫中實作，且不應將任何短期權杖硬編碼於網站。

後端公開端點已在受限主機白名單內讀取 Apple Music 首頁、播放器資料與全部 `next` 分頁。開發預覽實測原有「必聽新歌」由首批 100 首提升為 496 首，頁面歌曲計數與 CSV、JSON、TXT 匯出按鈕均同步顯示 496 首。
